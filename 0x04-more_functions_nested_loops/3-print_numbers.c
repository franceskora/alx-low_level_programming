#include <stdio.h>
#include "main.h"
/**
 * print_numbers - a function that prints the numbers from 0 to 9
 * user input's number prints to 9, regardless < 9 or > 9
 * Return: Always 0 (Success)
 */
void print_numbers(void)
{
	printf("%d");
	return (0);
}

